# Application Environment
NODE_ENV=development

# Supabase Configuration
SUPABASE_URL=https://your-project-id.supabase.co
SUPABASE_ANON_KEY=your-supabase-anon-key
SUPABASE_SERVICE_KEY=your-supabase-service-key

# Authentication
AUTH_SECRET=your-auth-secret

# DashScope AI Configuration (Server Side)
# Required for AI features - Get these from your Alibaba Cloud account
DASHSCOPE_API_KEY=your-dashscope-api-key-goes-here
QWEN_API_URL=https://dashscope-intl.aliyuncs.com/compatible-mode/v1
QWEN_MODEL=qwen-plus
QWEN_EMBEDDING_MODEL=text-embedding-ada-002

# API Server Configuration
API_PORT=8081

# Vite Configuration
VITE_PUBLIC_APP_URL=http://localhost:5173

# IMPORTANT NOTE:
# ---------------
# DashScope AI API access should ALWAYS go through the API proxy server
# for proper authentication and security. Never expose your API key
# to the client side code. The following variables are used only
# by the server-side API proxy.

# Development Commands:
# --------------------
# Start API proxy server: npm run api-server
# Start Vite dev server: npm run dev
# Start both together: npm run dev:all
# Test API connection: npm run test:api

# Production deployments should use environment variables in the deployment platform
# and should NOT include API keys in client-side code. 